# NutriLife — AI Context

Static website (HTML/CSS/JS) motivating Bulgarians toward healthy living. Core flow plus utility product pages, dark theme. Python scrapers for weekly grocery offers and supplement active-dose pricing. All content in Bulgarian.

## Quick orientation

```
index.html → move.html → eat.html → start.html
"Оправдания"  "Движение"  "Хранене"    "Старт"

smart-food.html
"Smart Food" — отделен utility page/project за оферти и умно пазаруване, линкнат от NutriLife

smart-supplements.html
"Smart Supplements" — отделен utility page/project за цена на активна доза при добавки
```

Main navbar principle: keep one top-level nav across product/core pages, with the money/product utilities visible as siblings: `Умна храна` then `Умни добавки`. Do not add `Добавки` as a separate top-level item again unless there is a deliberate IA change; the old `supplements.html` page is supporting educational content.

**Live site**: https://stop-excuses.github.io/nutrilife/  
**Repo**: https://github.com/stop-excuses/nutrilife  
**Hosting**: GitHub Pages, `master` branch

## Current working notes

Keep this section short and update it after hands-on QA/fixes so future sessions do not repeat the same discovery work.

**Token/context hygiene**
- Keep `AGENTS.md` short. Add only stable decisions, commands, and known traps that prevent rediscovery; avoid long research notes or full QA transcripts.
- Prefer updating the newest relevant note instead of appending a new long dated block for every small UI tweak.
- For Smart Supplements work, start from `js/supplements.js`, `scraper/supplement_scrapers.py`, `data/supplements.json`, and `tests/user-flow-smart-supplements.js`; do not re-audit labels from scratch unless product data changed.
- For Smart Food work, start from `js/offers.js`, `scraper/scraper.py`, `data/market_memory.json/js`, and `tests/user-flow-smart-food.js`; do not re-run broad scraper research unless sources changed.

**2026-05-22 nav decision**
- Top nav now has 8 visible items: `Оправдания`, `Проверка`, `Движение`, `Хранене`, `Умна храна`, `Умни добавки`, `Старт`, `Психично`.
- `Умни добавки` links directly to `smart-supplements.html`; generic `Добавки` is not in the main nav.
- Mobile navbar is intentionally one-line/all-visible, not horizontally auto-scrolled. Tested at 390px and 320px on `index.html`, `smart-food.html`, `smart-supplements.html`, `start.html`, `supplements.html`.

**2026-05-09 Smart Food product page QA**
- Local test URL used: `http://127.0.0.1:8000/smart-food.html` via `python -m http.server 8000 --bind 127.0.0.1`.
- Browser-tested `smart-food.html`: load, product cards, filter buttons, search, sort, pagination, price-comparison card links, favorite button, empty results.
- No console errors/warnings after the latest fix.
- Fixed product grid row stretching when one `.offer-card` is expanded by adding `align-items: start` to `.offers-grid` in `css/style.css`.
- Fixed empty filter/search state in `js/offers.js`: `renderOffers([])` now shows a full-row `.offers-empty` message plus `data-reset-offers` button that calls `resetOfferFiltersForNavigation()` and `applyFilters()`.
- Added BG i18n keys in `js/i18n.smart-food.js`: `sf.offers.empty`, `sf.offers.clear_filters`.
- Bumped cache params in `smart-food.html`: latest `css/style.css?v=18`, `js/i18n.smart-food.js?v=3`, `js/offers.js?v=27`.
- Mobile QA done at 390x844 and 480x844: navbar wraps without overlap, filters wrap, offer cards are one column, expanded details fit, empty-state reset works after a short scroll, no console errors/warnings.
- 2026-05-09 follow-up UX QA: searched common products (`яйца`, `кисело мляко`, `пиле`, `ориз`, `риба тон`, `зехтин`, `банани`) with useful results and no console errors.
- Reframed favorites as a lightweight shopping/watch list: visible copy now says `Пазарен списък`, heart button titles say add/remove from list.
- Improved price history: chart has current/previous/average/low stats, delta badge, clearer line chart, recent price rows, and mobile styling tested at 390x844.
- 2026-05-09 compact UX pass: expanded offer cards span 2 columns on desktop, details/images/history are tighter, and favorite rows open an inline preview in `#fav-panel` instead of navigating down to the offers grid.
- 2026-05-09 favorite product screen: favorite rows now open a full-screen product dialog with image, price facts, macros, store comparison, and price history; closes via X/backdrop/Escape. Cache params now `css/style.css?v=19`, `js/offers.js?v=28`.
- 2026-05-14 market memory pass: added `data/market_memory.json/js` as compact all-products + price-memory frontend data (~10MB vs ~18.8MB `all_products.js`), generated by `sync_offers.py`; `smart-food.html` now loads `market_memory.js`, while full `all_products.json/js` remains scraper/archive data. `js/offers.js` accepts compact price history arrays and includes catalog products in "Всички продукти".
- 2026-05-15 mobile QA/fix: `smart-food.html` crashed during price-comparison rendering when a comparison row had `new_price: null`; fixed `renderPriceComparison()` to skip products without any price and fall back to `price_per_kg` in store rows. Bumped `js/offers.js?v=43` and service worker cache to `nutrilife-v3`. Mobile tested at 390x844 on clean origin: Smart Food loads 36 offer cards + 12 comparison cards, search works; Smart Supplements loads 24 cards + 10 best cards, search `витамин` returns 93 products; no console errors.
- 2026-05-15 extended product QA: user-like mobile pass on Smart Food searches (`яйца`, `кисело мляко`, `пиле`, `ориз`, `риба тон`, `зехтин`, `банани`), empty state reset, source/store/category/sort/pagination, favorite list expand/show-more/product screen. Smart Supplements pass on searches (`креатин`, `витамин d`, `витамин c`, `омега`, `магнезий`, `протеин`, `цинк`), empty reset, D3 category, store/confidence/sort/pagination, best-card links. Fixed favorite show-more label toggle and supplement active-value units for keys like `creatine_mg_per_serving`; `smart-supplements.html` now loads `js/supplements.js?v=3`.
- 2026-05-15 premium visual pass: `smart-food.html` and `smart-supplements.html` now use image-led utility heroes, product chips, showcase tiles, and framed control panels; added `images/smart-supplements-hero.png`. Local mobile QA at 390x844 on `http://127.0.0.1:8002/`: Smart Food loads 36 cards, search `яйца` works, empty reset restores 36; Smart Supplements loads 24 cards, search `витамин d` returns 14, empty reset restores 24; no console errors/warnings.
- 2026-05-15 mobile paint fix: product result sections are excluded from global scroll-reveal because mobile could keep them visually transparent until first tap/repaint. `smart-food.html` and `smart-supplements.html` now load `css/style.css?v=28`, `js/main.js?v=12`, and `sw.js?v=8`; local 390x844 QA confirms product sections have no `data-reveal`, Smart Food loads 36 cards and Smart Supplements loads 24 cards immediately.
- 2026-05-15 global mobile reveal fix: on mobile/coarse pointer, `initScrollReveal()` now immediately marks reveal blocks as visible and CSS forces reveal elements visible at `max-width:480px`; all HTML pages now load `css/style.css?v=29`, `js/main.js?v=13`, and service-worker registrations use `sw.js?v=9`. Local 390x844 QA covered index, move, eat, start, mental, health-check, supplements, smart-food, smart-supplements, cheap redirect: every page had `unrevealed: 0`, no console errors; product cards still load (Smart Food 36, Smart Supplements 24).
- 2026-05-15 PWA pass: rewrote `manifest.json` with BG metadata, standalone display, PNG + maskable icons, and shortcuts; added `images/pwa/*`, `offline.html`, Apple mobile web app tags, and `sw.js` app-shell/offline fallback. Pages now load `css/style.css?v=30`, `js/main.js?v=14`, and `sw.js?v=10`. Local PWA QA on `http://127.0.0.1:8005/index.html`: manifest parses, icon/offline/sw assets return 200, page has manifest/apple/theme tags, no console errors.
- 2026-05-15 scraper Actions failure: scheduled `Scrape Offers` runs were not failing in scraping/validation; they failed after `sync_offers.py` because `data/market_memory.json/js` were regenerated but not staged before `git pull --rebase`, leaving unstaged changes. Fixed `.github/workflows/scraper.yml` to add `data/market_memory.json` and `data/market_memory.js` with the other generated offer files.
- 2026-05-15 PWA polish: converted `images/smart-supplements-hero.png` to `images/smart-supplements-hero.webp` (~1.8MB -> ~71KB) and updated HTML/CSS references; added mobile PWA install banner/hint in `js/main.js` with CSS styling and BG i18n keys. Pages now load `css/style.css?v=31`, `js/main.js?v=15`, `sw.js?v=11`. Local 390x844 QA: index/Smart Food/Smart Supplements have manifest + Apple tags, `unrevealed: 0`, no console errors, Smart Food 36 cards, Smart Supplements 24 cards. Manual GitHub workflow dispatch from local REST failed with 401 (no auth/token in environment); use GitHub UI `Scrape Offers -> Run workflow -> stores=all` to validate immediately, or wait for next schedule.
- 2026-05-16 T-Market scraper fix: `tmarketonline.bg` now returns Cloudflare 403/JS challenge to Python/Playwright, so `scrape_tmarket_text()` uses the official T-Market KZP CSV from `https://ftp.cloudcart.com/tmarket_kzp/viewer.php?embed=1&clean=1&download=1` first, converts EUR to BGN, deduplicates by product code, and only falls back to old shop pages if CSV fails. Local `python scraper/scraper.py --store tmarket` produced 773 merged T-Market offers.
- 2026-05-16 Smart Supplements QA/fix: GymBeam pages expose prices/stock in JSON-LD `AggregateOffer`; old fallback regex grabbed unrelated numbers like dimensions (`20.00 см`) as price. `scraper/supplement_scrapers.py` now flattens nested offers, skips out-of-stock offers, stores `availability_status`, and uses offer names to parse variant package weight. Updated GymBeam generated data: no remaining GymBeam `20.00` placeholder prices; `Protein 80 Plus - Weider` is `170.06 лв`, `66` servings, `2.68 лв / 25 g протеин`. Frontend filters `out_of_stock` rows and sorts value-first with a small confidence penalty. Local QA on `http://127.0.0.1:8000/smart-supplements.html`: protein view, GymBeam filter, search, empty state; no console errors.
- 2026-05-16 protein label follow-up: re-parsed existing protein rows with the improved scraper. HealthStore text labels now mark Mutant 2270/4540, Myprotein Impact Whey Isolate 1000g, and Optimum Nutrition Gold Standard 780g as `high` confidence with real per-serving protein. Frontend low-confidence warning now says the scraper could not automatically read clear protein grams, instead of implying the product has no label.
- 2026-05-16 supplement scraper optimization: `scraper/supplement_scrapers.py` now supports targeted runs: `--source`, `--category`, direct `--url`, `--concurrency`, optional `--cache`, safe `--merge-existing`, and explicit `--replace-scope` when a whole selected source/category should be replaced. Use targeted runs for QA fixes, e.g. `python scraper/supplement_scrapers.py --source HealthStore --category protein --per-category 8 --concurrency 4 --merge-existing`.
- 2026-05-21 Smart Food user-flow QA: added `tests/user-flow-smart-food.js` that mimics a real user (8 searches, all filter/sort/diet/source combos, pagination forward+back, card expand, favorite add, price comparison, mobile overflow) on desktop 1440x900 and mobile 390x844. Run: `node tests/user-flow-smart-food.js` with `python -m http.server 8000` up. Result: 49/49 steps pass, 0 console errors/warnings. Catalog has 1533 promos / 15064 products / 6 stores; mobile body width matches viewport (390px), no overflow. Combined with previous 40-test Playwright suite, smart-food has no outstanding issues.
- 2026-05-21 Smart Supplements label QA/fix: user reported that some cards say there's no readable label while the product page does show one. Built `scripts/audit_supplement_labels.py` (read-only re-extract via requests) and `scripts/refix_supplement_labels.py` (re-extract + write back, optional `--playwright` for JS-rendered/blocked stores). Found 4 mismatches in `protein/low`: Remedium Optimum Gold Standard 909g/2270g and Framar Thorne Vegan 676g/630g had clear `protein_g` per serving (24-30g) and were upgraded `low → high`. After fix protein high went 5 → 9, low 17 → 13 (remaining 13 are Fitness1 whose site currently returns HTTP 500 to all clients incl. headed Chromium — keep them with the estimate-style warning). Reworded `renderProteinWarning()` in `js/supplements.js`: when we have an `estimated_*` value it now says "Ориентировъчна сметка: 85%..." instead of the older "Не успяхме автоматично да прочетем", and only true no-label items get the no-label copy. Bumped `smart-supplements.html` to `data/supplements.js?v=6`, `js/supplements.js?v=9`. Added `tests/user-flow-smart-supplements.js`: 43/43 steps pass on desktop 1440x900 + mobile 390x844 (initial load, confidence labels, protein category, high-confidence has no warning, low-confidence has warning, no old copy, confidence filter, 8 searches, empty state, sorts, watch/compare, mobile overflow), 0 console errors/warnings.
- 2026-05-21 Smart Supplements per-page label verification: user asked to actually open every product page and check if the site shows a label (each site is different) before claiming "no label". Added `scripts/deep_audit_labels.py` that fetches each low/medium row (requests + Playwright for Fitness1/GymBeam/SilaBG) and prints the actual nutrient snippets found on the page. Audit found 4 fiber low rows whose pages clearly listed grams of fiber per serving (Framar АКАЦИЕВИ ФИБРИ 300g `8g`, Framar ГУАРОВИ ФИБРИ 225g `7.5g`, Framar ПСИЛИУМ ХУСК 500g `2.5g per чаена лъжичка`, GymBeam Daily Fiber `9g`) but the old fiber extractor returned empty `active={}` because `extract_named_g` was matching unrelated big numbers from the product title (`500 г`) instead of the per-serving value. Added `extract_fiber_g_per_serving()` in `scraper/supplement_scrapers.py` with serving-anchor + tabular `Фибри X g` patterns and a 1–20 g sanity range; fiber `medium → high` when an explicit per-serving value is found. Re-ran `python scripts/refix_supplement_labels.py --confidence low,medium --playwright` → 5 upgrades (4 fiber low→high above + 1 HealthStore NOW Psyllium Husk 500мг capsules `medium→high` with `fiber_g=1.1` for 5-capsule serving). Re-synced via `python sync_supplements.py`. Final distribution: high 174 (+5), low 15 (-4); fiber high 0→5, fiber low 6→2. Remaining 13 protein low + 2 fiber low are real "no per-serving label on page" cases — Fitness1 site returns HTTP 500 to all clients (Python + headed Chromium), and SilaBG Thorne / 3 HealthStore protein pages show serving grams but no explicit "X g protein per dose" text. Bumped `smart-supplements.html` to `data/supplements.js?v=7`, `js/supplements.js?v=10`. `node tests/user-flow-smart-supplements.js`: 43/43 steps pass on desktop 1440x900 + mobile 390x844, 0 console errors/warnings.
- 2026-05-22 Smart Supplements store/promo expansion: added FHL and Stayfit to `scraper/supplement_scrapers.py`; FHL needs listing-page discovery from `.gall-products`, while Stayfit uses `https://stayfit.bg/sitemap/product/1.txt`. Added promo extraction fields `old_price_bgn`, `discount_pct`, `promo_label` and frontend promo badges. Re-scraped `--source FHL --source Stayfit --per-category 8 --concurrency 4 --merge-existing --replace-scope`, then `python sync_supplements.py`: dataset is 333 products / 9 stores, with FHL 33, Stayfit 34, and 37 promo-tagged products. `node tests/user-flow-smart-supplements.js`: 43/43 pass, 0 console errors/warnings.
- 2026-05-22 Smart Supplements price/label correction: FHL product pages must parse the main `Цена:` text, not arbitrary `.price` blocks, or products such as Carnivor can pick related-card prices. Supplement cards now show product price in BGN/EUR, hide unknown availability instead of showing "непотвърдена", and seed `price_history` on refreshed rows. HealthStore GymBeam True Whey label is visible only in the second image; added a targeted label override (`22 g` protein) until image OCR is available in the scraper. Latest `node tests/user-flow-smart-supplements.js`: 43/43 pass, 0 console errors/warnings.
- 2026-05-22 Smart Food package/price-per-kg fix: weight parser was stripping `до 500 г` inside words such as `Балдо 500 г`; require a word boundary before `до`. Added multipack parsing for `20 бр x 2 г`, sync-time repair for missing/wrong package weights, and atomic writes for `market_memory.js`. Re-ran `python sync_offers.py`; examples fixed: Billa `Oberon Ориз Балдо 500 г` → `500 г`, `4.30 лв/кг`; Fantastico `ЧАЙ AHMAD ... 20 бр X 2 г` → `40 г`. Smart Food tests: targeted Playwright sort/favorites 3/3 pass; `node tests/user-flow-smart-food.js` 49/49 pass, 0 console errors/warnings.
- 2026-05-22 Smart Food protein ranking: restored `#protein-ranking` as a visible section titled `Качествен протеин за евро`. Keep it as a short practical shortlist (up to 8), not a blind top 10: dedupe by product type, cap category repetition, exclude obvious false positives (`спагети`, `пица`, `пекарна`, etc.), require plausible лв/кг and macros. `imgproxy-retcat.assets.schwarz` images are treated as broken and fall back locally to avoid 404 console errors. Latest checks: protein ranking targeted tests pass; `node tests/user-flow-smart-food.js` 49/49 pass, 0 console errors/warnings.
- 2026-05-23 Smart Supplements category expansion: added electrolytes (`?? / ???? ???????????`), collagen (`?? / 10 g ???????`), and iron (`?? / 14 mg ??????`). Keep electrolytes conservative: exclude ready drinks/BCAA/EAA/carbo products unless they expose a clear supplement-style serving. Collagen needs explicit collagen grams/total collagen, not package weight fallback. Latest targeted scrape: 371 products total, new categories have 9 electrolytes / 17 collagen / 11 iron. Cards now include a separate `???????? ? ??????` details block with active label values, package facts, confidence/form, and price basis. `node tests/user-flow-smart-supplements.js`: 57/57 pass, 0 console errors/warnings.
- 2026-05-28 Smart Food UX fix: moved `#fav-section` and the product search/filter block directly after the hero, before visual/intro sections, so watch list and search are near the top. Narrowed `isWatchoutOffer()` to clear junk/ultra-processed/processed-meat/sweetened instant coffee signals; do not use generic `health_score <= 5` as a "trap" reason. Local `node tests/user-flow-smart-food.js`: 49/49 pass, 0 console errors/warnings; browser check confirmed old examples like olives/butter/seafood salad no longer appear and no `health score` copy remains.
- 2026-05-28 Smart Food simplification: Smart Food is now mainly a utility screen (hero → `#fav-section` → search/results). Advanced diet/store/category filters are collapsed under `.advanced-filter-panel`; removed the editorial weekly shortlist/recommendation sections from Smart Food because they overstate confidence in noisy catalog data. Educational food context moved to `food-info.html` linked from Smart Food. `node tests/user-flow-smart-food.js`: 49/49 pass, 0 console errors/warnings; browser checked Smart Food and `food-info.html`.
- 2026-05-28 cheap redirect cleanup: `cheap.html` is now a minimal redirect/fallback page to `smart-food.html`; it must not load `offers.js` or contain old recommendation sections.
- 2026-05-29 Smart Food egg search fix: dessert/seasonal products like `Вафлени яйца LAMBERTZ` and `Raffaello Яйце` are excluded from the `яйца` intent and treated as junk/sweets in future scraper classification. `node tests/user-flow-smart-food.js`: 53/53 pass, 0 console errors/warnings; targeted favorite-list check for `яйца` shows 2 real egg rows, 0 dessert rows.
- 2026-05-29 product cache fix: `sw.js` uses cache-first for versioned assets and stale-while-revalidate for heavy product data (`data/market_memory.js`, `data/offers.js`, `data/supplements.js`). Do not use `fetch(..., {cache:'no-store'})` for product data; it forces every Smart Food/Supplements load to re-download MBs. Browser QA on `sw.js?v=33`: Smart Food caches `market_memory/offers` and reloads 36 cards offline; Smart Supplements caches `supplements.js` and reloads 24 cards offline; both with 0 console errors. Local QA skips external analytics/counter and uses local image fallbacks for broken/blocked product image hosts.
- 2026-06-18 Smart Food card redesign (pazarko.bg style): `.offer-card` is now vertical — full-width white image area on top, info body below. Discount badge is overlaid top-left on the image; fav button overlaid top-right. Grid: 4 cols >1200px, 3 cols >480px, 2 cols ≤480px (was 1 col). Light mode: white cards with shadow. Dark mode: dark-green card + white image area (high contrast). Mobile aspect-ratio 4:3 (was 1:1 = too tall). New PWA icons (flat plant/leaf design, all 5 sizes) generated by `scripts/generate_icons.py` using Pillow. 53/53 tests pass, 0 console errors. Do NOT add back the old horizontal layout (image left, info right) to `.offer-card`.
- 2026-06-20 SW perf fix: `sw.js` `isVersionedAsset` is now checked BEFORE `isProductData`, so versioned `data/offers.js?v=N` / `market_memory.js?v=N` use cache-first (no re-download every visit) instead of stale-while-revalidate. SW bumped to `v=35` across all HTML. This was the fix for "data re-downloads on every click".
- 2026-06-20 Smart Food fake-discount detector: `evaluateDiscountHonesty()` + `renderDiscountHonesty()` in `js/offers.js` compare each promo's claimed −X% against its own price memory → ✅ `Честно намаление` (price clearly below recorded avg / at historical low) or ⚠️ `Завишена база` (price ≈ normal, or claimed old_price inflated >10% above avg, e.g. Ocean Риба тон −34%). Stays silent unless ≥2 distinct observed prices. To make it work on first paint without the 34MB catalog, `sync_offers.py` now inlines compact price memory (avg/lowest/8-point history) into every offer → `data/offers.js` 6.5→8.2MB (cache-first, fine). `renderTopDeals()` now drops `weak`/завишена-база promos and ranks by real saving vs avg (`dealQualityScore`) instead of raw discount_pct. Bumped `css/style.css?v=54`, `data/offers.js?v=7`, `js/offers.js?v=80`. CSS: `.discount-honesty.dh-real/.dh-weak`.
- 2026-06-20 cleanup: deleted `smart-medicines.html`+`medicines.js`/`data/medicines.js`/`i18n.smart-medicines.js` (medicine price comparison = regulatory risk, never in nav) and `preview_offers.html` (dev artifact). Untracked `data/image_cache`, `data/brochure_cache`, `data/backups` (already in `.gitignore`, kept on disk). `cheap.html` kept as redirect.
- 2026-06-20 test note: sorting is a `<select id="sort-select">`; the `.sort-btn[data-sort]` buttons are `display:none` (JS-compat only). Tests must drive sort via `page.selectOption('#sort-select', ...)`, not clicks. `node tests/user-flow-smart-food.js`: 53/53 pass. Console errors ~245-249 = pre-existing broken external product-image hosts that fall back to local SVG (verified by A/B against pre-change HEAD: 245 before, 249 after); 0 broken images in live preview. Not a regression — do not chase per-image 404s.
- Smart Supplements already has buy links (`buildOutboundUrl` → "Към магазина", `data-affiliate-out`) and best-by-category cards; price sparkline (`renderSparkline`) already exists on Smart Food cards. Don't re-implement these.
- Not feasible without scraper work: a "до изчерпване/лимитирано количество" badge — brochure data carries no such signal (0 hits across offers).
- Remaining nice-to-have: a true cart with quantities would be a separate feature from the current watch list.

## Files

```
index.html / move.html / eat.html / start.html   Core flow pages
smart-food.html   Separate utility page/project for offers and smarter shopping
smart-supplements.html Separate utility page/project for supplement active-dose pricing
css/style.css        Single CSS file — entire design system
js/main.js           Accordion, age selector, sliders, tracker
js/offers.js         Offers: load, filter, bulk, profile recommendations
js/supplements.js    Supplements: load, filter, sort, active-dose cards
js/i18n.js           Active BG translations only
js/i18n.smart-supplements.js BG translations for Smart Supplements page
js/i18n.en.parked.js Parked EN translations — do not load/edit for normal BG work
data/offers.json     Weekly offers data (raw JSON) — generated, do not edit manually
data/offers.js       Weekly offers data (JS variable) — generated by sync_offers.py
data/supplements.json Price-per-active-dose supplement data — generated, do not edit manually
data/supplements.js   Supplement data JS variable — generated by sync_supplements.py
sync_offers.py       Sync offers.json → offers.js after scraping
sync_supplements.py  Sync supplements.json → supplements.js after supplement scraping
scraper/scraper.py   Main scraper — broshura.bg via Playwright
scraper/supplement_scrapers.py Supplement scraper — product sitemaps + JSON-LD + label text
scraper/NOTES.md     Scraper decisions, known issues, tested approaches
```

**Maintenance-only scripts** (run manually when updating content/translations, not part of regular pipeline):
```
translate_accordions.py   Add BG→EN translations for accordion bodies
translate_all.py          Add BG→EN translations for all other content blocks
update_i18n.py            Initial setup of data-i18n attributes across all HTML pages
```

## Coding rules

**General**
- Prefer proven libraries over hand-rolled code. Use `Fuse.js` for fuzzy search, `BeautifulSoup`/`requests` for HTML extraction, `pdfplumber` for PDFs.
- Be proactive and persistent: don't stop at "tool unavailable" if the next practical step is to install it, find its direct path, use an authenticated connector, or document the exact credential/login blocker. Avoid lazy handoffs.
- Don't add features, comments, error handling, or abstractions beyond what the task requires.
- Don't create new files unless necessary — extend existing ones.
- No backwards-compatibility shims. If something is unused, delete it.
- When a big problem takes multiple sessions to solve, document what was tried in a `NOTES.md` next to the relevant code.

**HTML/CSS**
- One CSS file (`css/style.css`) — no inline styles, no new CSS files.
- Reuse existing components: `.stat-card`, `.accordion-item`, `.info-box`, `.btn-green`, etc.
- Color modifiers `.green`, `.red`, `.amber`, `.purple` work on most components — use them instead of custom colors.
- All new text must have `data-i18n` or `data-i18n-html` attribute pointing to a key in `js/i18n.js`.
- Current copy workflow is BG-only. Do not add or update EN keys during normal content edits.
- `js/i18n.en.parked.js` exists only to preserve past EN work. Do not load it, merge it back, or spend tokens reading/editing it unless the task is explicitly about EN translations or re-enabling multilingual support.
- Responsive breakpoint is 480px — test mobile after layout changes.

**JavaScript**
- No frameworks — vanilla JS only. No new script files without a strong reason.
- DOM queries go inside `DOMContentLoaded` or the relevant `init*()` function.
- Tracker data uses localStorage key `nutrilife-tracker-YYYY-MM-DD`.
- Don't duplicate offer filtering logic — extend `renderOffers()` in `offers.js`.

**Python (scraper)**
- Read `scraper/NOTES.md` before changing scraper code.
- `data/offers.json` and `data/all_products.json` are generated — never edit manually.
- After any scraper run, always sync with `python sync_offers.py`.
- Scraper uses `SOURCE_PRIORITY`: structured DOM (3) > brochure listing (2). New sources must fit this ranking.
- Don't add new per-store scrapers to `scraper.py` directly — add to `scraper/store_scrapers.py` and import.
- Temporary/debug data files go in `data/` with a clear name prefix. Delete them when done — don't commit them.

## Scraper workflow

```
python scraper/scraper.py   # → data/offers.json
python sync_offers.py       # → data/offers.js (required for GitHub Pages)
```

Supports: Lidl, Kaufland, Billa, Fantastico, T-Market, Dar. Kaufland also has a PDF scraper (`kaufland_pdf_scraper.py`). Read `scraper/NOTES.md` before touching scraper code.

## Supplement scraper workflow

```
python scraper/supplement_scrapers.py   # → data/supplements.json
python sync_supplements.py              # → data/supplements.js (required for GitHub Pages)
```

Sources: Fitness1, HealthStore, Remedium, Framar, SilaBG, GymBeam, MyPharmacy, Mirabel, Ozone. Data is for price transparency only: price per active unit such as 5 g creatine, 1000 mg EPA+DHA, 100 mg magnesium, 1000 IU vitamin D3, 25 g protein, or 5 g fiber. Do not present supplements as medical advice or personalized recommendations. Mirabel/Ozone are included as best-effort sources but may produce no usable records if their product pages do not expose active-dose text.

## Colors

| Name | Hex | Usage |
|---|---|---|
| Background | `#0a0a0a` | body |
| Text | `#f0efe8` | primary |
| Muted | `#b0afa6` | paragraphs |
| Green | `#3B6D11` / `#97C459` | positive, buttons, accents |
| Red | `#E24B4A` | negative, statistics |
| Amber | `#BA7517` | warning |
| Purple | `#7F77DD` / `#534AB7` | page 5 theme |

Card backgrounds: red `#110a0a`/`#2a1414`, green `#0d1a08`/`#2a3d1e`, purple `#0f0d1a`/`#2a2442`, amber `#1a1408`/`#3d2e14`

## CSS components (style.css)

| Class | ~Line |
|---|---|
| `.navbar`, `.dot` | 104 |
| `.stat-box`, `.stat-card` | 198, 239 |
| `.cards-grid` | 270 |
| `.accordion-item` | 286 |
| `.stat-pill` | 363 |
| `.research` | 402 |
| `.progress-bar` | 418 |
| `.age-btn`, `.age-content` | 465 |
| `.timeline` | 508 |
| `.info-box` | 551 |
| `.cta-section` | 575 |
| `.btn-green/.btn-red/.btn-purple` | 619 |
| `input[type="range"]` | 683 |
| `.comparison` | 717 |
| `.offer-card` | 770 |
| `.filter-btn` | 880 |
| `.tracker-item` | 911 |
| `.week-chart` | 972 |
| `.meal-card` | 1070 |
| `.bulk-card` | 1094 |
| `.savings-box` | 1118 |
| `.shopping-list` | 1135 |

Color modifiers `.green`, `.red`, `.amber`, `.purple` work on most components.  
Utilities: `.mt-16/.mt-24/.mt-32`, `.hidden`, `.fade-in`. Responsive breakpoint: 480px.

## JS functions

**main.js**: `initAccordions()`, `initAgeSelector()`, `initSliders()`, `initProgressBars()`, `initTracker()`, `updateTrackerProgress()`, `updateWeekChart()`, `window.updateSteps()`, `window.updateProtein()`

**offers.js**: `loadOffers()`, `renderOffers()`, `renderBulkRecommendations()`, `renderProfileRecommendations()`, category/profile filter handlers

**Data attributes**: `data-age`, `data-output`, `data-fn`, `data-width`, `data-habit`, `data-category`, `data-profile`

## Offer JSON schema

```json
{
  "id": "string", "store": "Lidl|Kaufland|Billa|T-Market",
  "name": "string", "emoji": "🥚",
  "category": "protein|canned|grain|legume|dairy|nuts|fat|vegetable",
  "new_price": 3.49, "old_price": 4.29, "discount_pct": 19,
  "valid_until": "2026-01-19",
  "weight_raw": "500г", "weight_grams": 500, "price_per_kg": 6.98,
  "shelf_life": "1-2г|малотраен|...", "is_bulk_worthy": true,
  "health_score": 1-10,
  "diet_tags": ["high_protein","keto","mediterranean","vegetarian","budget"]
}
```

Health scores: 10 (риба тон, зехтин) → 9 (яйца, пилешко, леща, нахут) → 8 (овес, скир) → 7 (кисело мляко, сирене) → 6 (ориз, хляб)

## Page sections reference

**index.html** (488 lines): Hero (22) → Огледалото stats (34) → Age buttons (63) → Accordions (154) → Timeline (403) → CTA (466)

**move.html** (314 lines): Hero (22) → 6 accordions мозък/настроение/сън/хормони/сърце/стави (34) → Тежести/HIIT (176) → Крачки slider (241) → Мускули (270) → CTA (291)

**eat.html** (329 lines): Hero (22) → 3 принципа (44) → Топ 15 храни accordions (147) → Протеин калкулатор slider (277) → Примерен ден (291) → CTA (322)

**smart-food.html** (~190 lines): Hero → Пазарен списък `#fav-section` → Search/filters + `#offers-grid`; no editorial shortlist/recommendation blocks.

**food-info.html** (~180 lines): Supporting food info page linked from Smart Food; holds visual/context blocks, ден по ден, and cost-of-illness content.

**start.html** (317 lines): Hero (22) → 7-дневен план accordions (46) → Tracker 6 checkboxes localStorage (230) → Седмичен chart (272) → CTA (305)
